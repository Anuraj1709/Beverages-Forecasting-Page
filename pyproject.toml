[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "beverage-forecasting"
version = "0.1.0"
description = "End-to-end weekly beverage sales forecasting service."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
